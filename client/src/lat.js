const themeSEttings = (mode) => {
    return {
        pallete: {
            ...(mode==='dark' ? {
                primary:{
                    dark, medium, SVGFESpotLightElement
                },
                neutral:
                background:,
            }:{
                //light
            })
        }
        typhography
    }
}